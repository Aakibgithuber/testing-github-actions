name: Airflow Pipeline Deployment

on:
  push:
    branches:
      - main
      - dev_**
  workflow_dispatch:
    inputs:
      branch:
        description: 'Branch to Deploy'
        required: true
        default: 'main'
      environment:
        description: 'Deployment Environment'
        required: true
        default: 'dev'
        type: choice
        options:
          - dev
          - prod

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup SSH Key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H ${{ env.SERVER_HOST_DEV }} >> ~/.ssh/known_hosts
          ssh-keyscan -H ${{ env.SERVER_HOST_PROD }} >> ~/.ssh/known_hosts

      - name: Deploy to Server
        run: |
          if [ "${{ github.event.inputs.environment }}" == "prod" ]; then
            SERVER_USER=${{ env.SERVER_USER_PROD }}
            SERVER_HOST=${{ env.SERVER_HOST_PROD }}
            GCP_ZONE=${{ env.GCP_ZONE_PROD }}
          else
            SERVER_USER=${{ env.SERVER_USER_DEV }}
            SERVER_HOST=${{ env.SERVER_HOST_DEV }}
            GCP_ZONE=""
          fi

          echo "Connecting to server: $SERVER_HOST"
          ssh -i ~/.ssh/id_rsa $SERVER_USER@$SERVER_HOST << EOF
            set -e
            echo "Switching to root user"
            sudo -i
            
            echo "Navigating to Pipeline Repository"
            cd /home/ai-pipeline/Navirego-AI-Pipeline/
          EOF
